exit
connect <username> <password> <host> <port> <updatePort> // Make updatePort optional and use predefined if not specified?
create updateListener <port> // Can be removed, since a updateListener is started by a successful connect
create debt <amount> "<what>" <to/from> "<to/from username>" "<comment>" // Make comment optional?
<accept/decline> debt <ID>
ls <debts/friends>
complete debt <ID>
<accept/decline> friend <username/ID?>
